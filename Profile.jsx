import { View } from "react-native";
import { Title } from "react-native-paper";


const Profile = (props) => 
{
    return(
        <View>
            <Title>Profile</Title>
        </View>
    )
}

export default Profile;